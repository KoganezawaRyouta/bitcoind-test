version: '2'

services:
  bitcoind:
    build:
      context: .
      dockerfile: ./containers/bitcoind/Dockerfile
    environment:
      BITCOIND_VERSION: v0.16.2
    mem_limit: 15gb
    cpuset: 0,1,2,3
    ports:
      - "8332:8333"
      - "18332:18333"
    tty: true
    stdin_open: true

